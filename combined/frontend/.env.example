# ── Supabase (unused on the combined backend — frontend still imports) ──
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=

# ── TrustScan API ────────────────────────────────────────────────────
# In local dev leave this BLANK — the Vite proxy forwards /api to
# http://localhost:8000 (see vite.config.ts).
# In production, set to the absolute backend origin, e.g.
#   VITE_TRUSTSCAN_API=https://api.trustscan.ai
VITE_TRUSTSCAN_API=
